{
    "version": "0.0.1",
    "general": [
        {
            "appName": "", // default is like a catalog name
            "projectWorkspace": "." // default value
        }
    ],
    "code": [
        {
            "includeDir": [
                "$PATH" // default - build.py must find gcc/g++
            ],
            "includeDirExternal": [ // not needed (optional)
            ],
            "libs": [
                {
                "win32": [ // optional if only linux support

                ]
                },
                {
                    
                    "linux": [  // optional if only windows support

                    ]
                }

            ],
            "libsExternal": [ // not needed (optional)

            ]
        }
    ],
    "debug": [
        {
            "flags": [ // needed
                "-g",
                "-Wall",
                "-Wextra"
            ],
            "definitions": [ // optional

            ]
        }
    ],
    "release": [
        {
            "flags": [ // needed
                "-O3",
                "-Wall",
                "-Wextra"
            ],
            "definitions": [ // optional

            ]
        }
    ]
}